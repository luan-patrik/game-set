export type detailPostSettings = {
  name: string
  filesettings: {
    authorId: string
    fileUrl: string
    name: string
    private: boolean
    size: number
    id: string
    createdAt: Date
  }[]
}
